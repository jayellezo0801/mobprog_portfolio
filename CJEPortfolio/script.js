var typed = new Typed(".multiple-text", {
    strings: ["Student", "Musician", "Vocalist"],
    typeSpeed: 100,
    backspeed: 100,
    backDelay: 1000,
    loop: true
})

var typed = new Typed(".single-text", {
    strings: ["USTP JASAAN"],
    typeSpeed: 100,
    backspeed: 100,
    backDelay: 1000,
    loop: true
})


var typed = new Typed(".education-text", {
    strings: ["Educational Record"],
    typeSpeed: 100,
    backspeed: 100,
    backDelay: 1000,
    loop: true
})

var typed = new Typed(".hobbies-text", {
    strings: ["Hobbies"],
    typeSpeed: 100,
    backspeed: 100,
    backDelay: 1000,
    loop: true
})

var typed = new Typed(".sport-text", {
    strings: ["Playing billiard"],
    typeSpeed: 100,
    backspeed: 100,
    backDelay: 1000,
    loop: true
})


